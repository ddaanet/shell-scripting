{
  "description": "Run shellcheck on shell files after Claude writes or edits them; full findings go to the model via additionalContext, the user sees only a one-line 'shellcheck failed (N lines)' summary.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/shellcheck-on-edit.sh\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
